listener 1883
protocol mqtt

listener 9001
protocol websockets

# Anonymous, deliberately: this experiment is about the binding, not about
# authentication. The specification's security page says per-body credentials
# and ACLs; that is experiment 006's problem, not this one's.
allow_anonymous true
persistence false
log_type notice
